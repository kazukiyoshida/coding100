// URL:
// start:
// end:

use itertools::Itertools;
use proconio::input;
use proconio::marker::Chars;
use std::cmp;
use std::collections::HashSet;

struct G {
    x: usize,
}

impl G {
    fn dfs(&mut self) {}
    fn main(&mut self) {}
}

fn main() {
    input! {
        x: usize,
    }

    G { x }.main();

    println!("{:?}", x);
}
